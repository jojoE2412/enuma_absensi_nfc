name: Deploy to Firebase Hosting
on:
  push:
    branches:
      - main # Ganti dengan branch utama Anda jika bukan 'main'
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install & Build
        working-directory: frontend
        run: |
          npm ci
          npm run build # Menjalankan build untuk aplikasi Web Anda

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ENUMA_ABSENSI_NFC }}
          projectId: enuma-absensi-nfc
          channelId: live
